---
import { portableTextToHtml } from '../utils/sanity';

interface Props {
  id: string;
  title: string;
  section?: {
    content?: any[];
  };
  bgClass?: string;
}

const { id, title, section, bgClass = 'section-light' } = Astro.props;

const content = Array.isArray(section) ? section : section?.content;
---

<section class={`section ${bgClass}`} id={id}>
  <div class="container-narrow">
    <h2>{title}</h2>
    <div class="section-content">
      {content && content.length > 0 && (
        <div class="block-text">
          <Fragment set:html={portableTextToHtml(content)} />
        </div>
      )}
    </div>
  </div>
</section>

<style>
  .block-text :global(p) {
    font-size: 1.15rem;
    line-height: 1.75;
    color: var(--color-text-dark-muted);
    margin-bottom: var(--space-sm);
  }

  .block-text :global(p:last-child) {
    margin-bottom: 0;
  }
</style>
